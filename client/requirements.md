## Packages
framer-motion | Page transitions, floating hearts, and scroll-triggered animations
canvas-confetti | For a cute celebratory confetti effect when adding a message
@types/canvas-confetti | Type definitions for canvas-confetti

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  sans: ["var(--font-sans)"],
}
